# AI 语音绘图工具设计文档

## 1. 设计目标

本项目目标是实现一款纯语音控制的绘图工具。用户不通过鼠标或键盘进行绘图操作，而是使用中文语音指令完成图形创建、对象编辑、撤销重做、清空画布、作品导出和工程保存。

设计重点：

- 语音指令能被稳定转换为结构化绘图命令
- 常见口语表达具备一定容错能力
- 用户说完后能看到画布变化和反馈
- 复杂指令能被拆解为多个原子操作
- 主分支在任意阶段保持可运行

## 2. MVP 指令能力

### 2.1 已计划并实现

| 能力 | 示例指令 | 实现状态 |
| --- | --- | --- |
| 创建圆形 | “画一个红色圆形” | 已实现 |
| 创建矩形 | “在右边画一个蓝色矩形” | 已实现 |
| 创建直线 | “画一条黑色直线” | 已实现 |
| 创建箭头 | “从圆形指向矩形画一个箭头” | 已实现 |
| 创建文本 | “写上开始两个字” | 已实现 |
| 修改颜色 | “把它改成绿色” | 已实现 |
| 修改大小 | “把它放大一点” | 已实现 |
| 移动对象 | “向右移动一点” | 已实现 |
| 删除对象 | “删除它” | 已实现 |
| 撤销操作 | “撤销” | 已实现 |
| 重做操作 | “重做” | 已实现 |
| 清空画布 | “清空画布” | 已实现 |
| 导出图片 | “导出为图片” | 已实现 PNG 导出 |
| 导出 SVG | “导出为 SVG” | 已在拓展阶段实现 |
| 导出 JSON | “导出为 JSON” | 已在拓展阶段实现 |
| 保存工程 | “保存工程” | 已在拓展阶段实现 |
| 加载工程 | “加载工程” | 已在拓展阶段实现 |
| 简单复合指令 | “画两个圆，并用箭头连接它们” | 已实现 |

### 2.2 MVP 暂未实现

| 能力 | 未实现原因 |
| --- | --- |
| 自由曲线绘制 | 纯语音描述曲线形状需要更复杂的参数化表达，不适合 MVP 第一阶段 |
| 高精度排版 | 需要布局算法和更多对象约束，当前优先保证基础绘图闭环 |
| 图层面板 | 图层可视化会引入额外鼠标交互，MVP 暂时使用最近对象上下文 |
| 云端保存 | 当前项目无后端服务，已先完成浏览器本地工程保存 |
| 导出 PDF | PDF 需要分页、尺寸和字体策略，留作后续交付增强 |
| 大模型语义解析 | 当前使用规则解析，便于控制准确性、延迟和测试稳定性 |

## 3. 指令理解设计

MVP 使用规则解析方式，流程如下：

```text
原始语音文本
  -> 文本规范化
  -> 复合片段拆分
  -> 动作识别
  -> 参数提取
  -> 结构化命令
```

结构化命令示例：

```json
{
  "type": "create",
  "shape": "circle",
  "style": {
    "fill": "#ef4444"
  },
  "position": {
    "region": "center"
  }
}
```

规则解析覆盖：

- 图形词：圆形、圆、矩形、方框、线、直线、箭头、文字
- 颜色词：红、蓝、绿、黄、黑、白、紫、橙、灰
- 位置词：中间、左边、右边、上方、下方、左上角等
- 动作词：画、创建、添加、改成、移动、删除、撤销、重做、清空、导出、保存、加载
- 程度词：大、小、放大、缩小、一点
- 连接词：连接、指向、从、到

## 4. 执行链路设计

```text
SpeechInput
  -> useDrawingStore.runCommandText
  -> CommandParser.parseCommand
  -> CommandNormalizer.normalizeCommands
  -> CommandExecutor.executeDrawingCommand
  -> CanvasEngine / ExportController
  -> FeedbackPanel + SpeechSynthesis
```

### 4.1 语音输入

`SpeechInput` 基于浏览器 Web Speech API：

- 语言设置为 `zh-CN`
- 开启临时识别结果
- 最终识别文本触发命令执行
- 识别错误转换为中文反馈

### 4.2 状态管理

`useDrawingStore` 保存：

- 画布对象列表
- 当前对象 ID
- 最近识别文本和临时识别文本
- 语音状态
- 撤销栈和重做栈
- 导出请求
- 浏览器本地工程快照
- 反馈消息队列

### 4.3 画布对象

画布对象使用统一数据模型：

```ts
type CanvasObject = {
  id: string;
  type: "circle" | "rect" | "line" | "arrow" | "text";
  x: number;
  y: number;
  width?: number;
  height?: number;
  radius?: number;
  style: ShapeStyle;
  text?: string;
};
```

渲染层使用 SVG。这样可以直接表达基础图形、箭头和文本，也方便导出时复用 SVG 序列化逻辑。

## 5. 容错与反馈

已实现的容错策略：

- 支持常见同义词，例如“圆”“圆形”“圈”
- 支持颜色简称，例如“红”和“红色”
- 支持常见位置描述，例如“右边”“右侧”
- 支持上下文代词“它”，默认指向当前对象
- 不支持的指令会返回示例建议
- 空画布导出时不会下载，而是提示“画布为空，无法导出图片”
- 空画布保存工程时不会覆盖本地快照，而是提示“画布为空，无法保存工程”
- 没有本地工程快照时加载工程会提示“没有可加载的工程”

反馈形式：

- 页面反馈列表
- 最近识别文本展示
- 临时识别文本展示
- 浏览器语音播报执行结果

## 6. 响应延迟设计

MVP 采用本地规则解析和前端内存状态执行，避免网络请求带来的不确定延迟。

预期响应：

- 简单绘图、编辑、移动：语音识别完成后快速执行
- 复合指令：拆分为多个命令顺序执行
- 导出 SVG：直接下载 SVG 序列化结果
- 导出 PNG：SVG 序列化后通过浏览器 Canvas 生成图片，耗时取决于浏览器和画布复杂度
- 导出 JSON：直接下载带版本字段的工程文件
- 保存/加载工程：使用浏览器 localStorage 保存和恢复最近工程快照

## 7. 测试设计

当前测试覆盖：

- 指令解析：`tests/parser.test.ts`
- 画布执行：`tests/executor.test.ts`
- 状态闭环：`tests/store.test.ts`
- 导出序列化：`tests/export.test.ts`

验证命令：

```bash
npm audit
npm run test
npm run build
```

当前测试结果：

- 安全审计：0 漏洞
- 单元测试：4 个测试文件，46 个测试
- 生产构建：通过

## 8. 原创与第三方边界

原创实现：

- 中文语音绘图指令解析规则
- 结构化绘图命令协议
- 画布对象模型
- SVG 画布渲染逻辑
- 语音识别到命令执行的状态闭环
- PNG/SVG/JSON 导出流程
- JSON 工程文件与本地工程快照逻辑
- 反馈和语音播报机制

第三方依赖用途：

- React / React DOM：前端 UI
- Zustand：状态管理
- lucide-react：图标
- Vite / TypeScript / Vitest：开发、构建和测试工具

第三方依赖不提供项目核心语音绘图业务逻辑。

## 9. 后续拓展方向

用户验证 MVP 后，可继续开发：

- 更复杂的复合指令
- 更强的对象定位，例如“左边那个圆”
- 图层与对象命名
- PDF 导出
- 云端项目保存与加载
- 流程图、思维导图模板
- AI 语义解析或自然语言布局优化
