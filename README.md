# AI 语音绘图工具

## 项目简介

本项目对应选题二：开发一款纯语音控制的绘图工具。用户不能使用鼠标或键盘，只能通过语音指令完成绘图创作、图形编辑、画布操作、样式调整和作品导出等流程。

项目重点不只是“能听懂命令”，还需要综合考虑：

- 语音指令理解的准确性与容错性
- 语音到绘图操作的响应延迟
- 复杂指令的拆解与执行能力
- 无鼠标、无键盘场景下的交互闭环
- 已实现能力、未实现能力与原因说明

## 目标用户与使用场景

目标用户包括希望通过自然语言快速创作草图、图示、流程图或简单插画的人群，也包括不方便使用传统输入设备的用户。

典型场景：

- 通过语音创建基础图形，例如圆形、矩形、线条、箭头
- 通过语音调整颜色、大小、位置、层级和透明度
- 通过语音组合多个简单操作，完成复杂图形创作
- 通过语音撤销、重做、清空画布、保存或导出作品
- 通过语音询问当前画布状态或选中对象信息

## 核心约束

- 用户不能使用鼠标。
- 用户不能使用键盘。
- 所有绘图操作必须由语音指令触发。
- 系统应对口语化表达、模糊描述和局部识别错误具备一定容错能力。
- 系统应在无法理解命令时给出明确反馈，并引导用户重新表达。

## 功能规划

### 基础绘图能力

- 创建直线、箭头、矩形、圆形、椭圆、多边形等基础图形
- 创建文本标注
- 设置填充色、描边色、线宽、字体大小等样式
- 移动、缩放、旋转和删除对象
- 撤销、重做、清空画布

### 语音交互能力

- 实时语音输入与语音识别
- 将自然语言转换为结构化绘图指令
- 支持单步指令，例如“画一个红色圆形”
- 支持复合指令，例如“在圆形右边画一个蓝色矩形，并用箭头连接它们”
- 支持上下文指令，例如“把它放大一点”“改成绿色”“向右移动一些”
- 对无法执行的指令进行语音或文本反馈

### 画布与对象管理

- 维护对象列表与唯一对象标识
- 支持当前选中对象或最近创建对象的上下文引用
- 支持对象层级调整，例如置顶、置底、前移、后移
- 支持画布缩放、居中、适配视图

### 导出与保存

- 保存项目数据
- 导出图片文件
- 后续可扩展为导出 SVG、PDF 或 JSON 工程文件

## 指令能力示例

| 指令类型 | 示例语音 | 预期行为 |
| --- | --- | --- |
| 创建图形 | “画一个红色圆形” | 在画布中创建红色圆形 |
| 指定位置 | “在左上角画一个蓝色矩形” | 在指定区域创建矩形 |
| 修改样式 | “把它改成绿色” | 修改当前对象颜色 |
| 移动对象 | “向右移动一点” | 移动当前对象 |
| 复合绘图 | “画两个圆，并用箭头连接” | 拆解为多个绘图步骤执行 |
| 撤销操作 | “撤销刚才的操作” | 回退上一步 |
| 清空画布 | “清空画布” | 删除全部对象 |
| 导出作品 | “导出为图片” | 生成图片文件 |

## 初步技术方案

具体技术栈后续可根据课程要求和实现周期调整，当前建议采用模块化设计：

```text
语音输入
  -> 语音识别
  -> 指令理解
  -> 指令规范化
  -> 绘图操作调度
  -> 画布渲染
  -> 反馈与状态更新
```

### 推荐模块划分

- `SpeechInput`：负责录音、语音识别和识别结果回调
- `CommandParser`：负责将自然语言解析为结构化指令
- `CommandExecutor`：负责执行绘图、编辑、撤销、导出等操作
- `CanvasEngine`：负责画布对象管理与渲染
- `StateManager`：负责历史记录、当前选中对象和上下文引用
- `FeedbackManager`：负责错误提示、确认反馈和状态播报

## 指令理解设计

为了提高准确性与容错性，指令理解可分为两层：

1. 规则解析：优先处理高频、明确、可枚举的绘图命令。
2. 语义解析：处理更口语化、复杂或上下文相关的表达。

结构化指令示例：

```json
{
  "action": "create",
  "shape": "circle",
  "style": {
    "fill": "red",
    "stroke": "black"
  },
  "position": {
    "region": "center"
  }
}
```

## 响应延迟目标

- 简单指令：尽量在 1 秒内完成解析与绘制
- 复合指令：尽量在 2 到 3 秒内完成拆解与执行
- 无法识别的指令：及时反馈原因，避免用户长时间等待

## 容错策略

- 支持同义词，例如“圆”“圆形”“画个圈”
- 支持模糊程度词，例如“一点”“大一些”“靠右一点”
- 支持上下文代词，例如“它”“刚才那个”“这个图形”
- 当关键信息缺失时主动追问，例如“你想画什么颜色？”
- 当识别结果置信度较低时请求用户确认

## 项目交付内容

- 可运行的 AI 语音绘图工具
- 项目 README 文档
- 设计文档，说明计划支持的指令能力、最终实现情况和未完成原因
- 产品文档：[`docs/product.md`](docs/product.md)
- 技术架构文档：[`docs/architecture.md`](docs/architecture.md)
- 必要的测试说明或演示材料

## 后续里程碑

1. 完成项目技术栈选择与基础工程搭建
2. 实现画布渲染和基础图形操作
3. 接入语音输入与语音识别
4. 实现基础指令解析与执行
5. 支持上下文编辑和复合指令拆解
6. 完成导出、保存和错误反馈
7. 编写设计文档与演示说明

## 当前状态

项目已初始化，当前阶段为基础代码架构搭建。后续将按照模块逐步实现语音输入、指令解析、画布引擎、状态管理、导出和测试能力。

## 本地运行

安装依赖：

```bash
npm install
```

启动开发环境：

```bash
npm run dev
```

构建生产版本：

```bash
npm run build
```

运行测试：

```bash
npm run test
```
